#!groovy

pipeline {
    stage("Prepare") { 
        steps {    
            println("Preparing...")
        }
    }

    stage("Build") { 
        steps {    
            println("Building...")
        }
    }

    stage("Test") { 
        steps {    
            println("Testing...")
        }
    }

    stage("Deploy") { 
        steps {    
            println("Deploying...")
        }
    }
}